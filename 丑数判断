#include "stdio.h"
#include "stdlib.h"
#include <iostream>

int main()
{
	int t;
	cout << "Type in your num:";
	cin >> t;
	while (t % 7 == 0)
		t /=7;
	while (t % 5 == 0)
		t /= 5;
	while (t % 3 == 0)
		t /= 3;
	while (t % 2 == 0)
		t /= 2;
	if (t == 1)
		cout << "Yes";
	else
		cout << "True";
}

